import 'package:flutter/material.dart';

class FirstStepBusinessAccountWidget extends StatelessWidget {
  const FirstStepBusinessAccountWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("hhhhhh")],
    );
  }
}